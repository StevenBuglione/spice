name: Agent workspace export

on:
  pull_request:
    branches:
      - main
    paths:
      - '**'
  push:
    branches:
      - agent/issue-8-type-aware-loading

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Package exact workspace
        run: tar --exclude=.git -czf /tmp/spice-workspace.tar.gz .
      - name: Upload workspace
        uses: actions/upload-artifact@v4
        with:
          name: spice-workspace-${{ github.event.pull_request.head.sha || github.sha }}
          path: /tmp/spice-workspace.tar.gz
          retention-days: 1
          if-no-files-found: error
