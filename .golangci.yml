version: "2"

run:
  timeout: 10m
  tests: true
  modules-download-mode: vendor

linters:
  default: none
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - containedctx
    - cyclop
    - depguard
    - durationcheck
    - errcheck
    - errorlint
    - exhaustive
    - fatcontext
    - forbidigo
    - forcetypeassert
    - gocheckcompilerdirectives
    - gocritic
    - godoclint
    - gomoddirectives
    - govet
    - ineffassign
    - interfacebloat
    - maintidx
    - modernize
    - nilerr
    - nilnesserr
    - nilnil
    - noctx
    - nolintlint
    - nosprintfhostport
    - predeclared
    - recvcheck
    - revive
    - rowserrcheck
    - sloglint
    - sqlclosecheck
    - staticcheck
    - testableexamples
    - thelper
    - tparallel
    - unconvert
    - unparam
    - unused
    - usestdlibvars
    - wastedassign
  settings:
    cyclop:
      max-complexity: 15
      package-average: 10
    depguard:
      rules:
        public-api:
          list-mode: lax
          files:
            - "annotation/**/*.go"
            - "async/**/*.go"
            - "cache/**/*.go"
            - "config/**/*.go"
            - "data/**/*.go"
            - "event/**/*.go"
            - "httpclient/**/*.go"
            - "lifecycle/**/*.go"
            - "management/**/*.go"
            - "migration/**/*.go"
            - "observability/**/*.go"
            - "retry/**/*.go"
            - "schedule/**/*.go"
            - "security/**/*.go"
            - "web/**/*.go"
          deny:
            - pkg: "github.com/StevenBuglione/spice/compiler"
              desc: "public API packages must not depend on compiler packages"
            - pkg: "github.com/StevenBuglione/spice/internal"
              desc: "public API packages must not depend on internal implementation packages"
            - pkg: "github.com/StevenBuglione/spice/cmd"
              desc: "public API packages must not depend on CLI packages"
            - pkg: "github.com/StevenBuglione/spice/starter"
              desc: "core runtime packages must not depend on opt-in starters"
        compiler:
          list-mode: lax
          files:
            - "compiler/**/*.go"
          deny:
            - pkg: "github.com/StevenBuglione/spice/internal/cli"
              desc: "compiler packages must not depend on the CLI"
            - pkg: "github.com/StevenBuglione/spice/cmd"
              desc: "compiler packages must not depend on command entrypoints"
            - pkg: "github.com/StevenBuglione/spice/starter"
              desc: "compiler packages must not depend on opt-in starters"
        starters:
          list-mode: lax
          files:
            - "starter/**/*.go"
          deny:
            - pkg: "github.com/StevenBuglione/spice/compiler"
              desc: "runtime starters must not depend on compiler packages"
            - pkg: "github.com/StevenBuglione/spice/internal"
              desc: "public starters must not depend on repository internals"
            - pkg: "github.com/StevenBuglione/spice/cmd"
              desc: "public starters must not depend on command entrypoints"
            - pkg: "github.com/StevenBuglione/spice/examples"
              desc: "public starters must not depend on example applications"
        cli:
          list-mode: lax
          files:
            - "cmd/**/*.go"
            - "internal/cli/**/*.go"
          deny:
            - pkg: "github.com/StevenBuglione/spice/examples"
              desc: "the CLI must not depend on example applications"
            - pkg: "github.com/StevenBuglione/spice/starter"
              desc: "the CLI must not depend on opt-in starters"
    errcheck:
      check-type-assertions: true
      check-blank: true
      exclude-functions:
        - "(io.Closer).Close"
    exhaustive:
      default-signifies-exhaustive: false
    forbidigo:
      analyze-types: true
      forbid:
        - pattern: '^fmt\.Print(f|ln)?$'
          msg: "write through an injected io.Writer or structured logger"
        - pattern: '^log\.Fatal(f|ln)?$'
          msg: "return an error to the entrypoint"
        - pattern: '^os\.Exit$'
          msg: "return an error and let an entrypoint select the exit code"
    govet:
      enable-all: true
      disable:
        - fieldalignment
    maintidx:
      under: 20
    nolintlint:
      allow-no-explanation:
        - funlen
      require-specific: true
      require-explanation: true
    revive:
      severity: warning
      rules:
        - name: comment-spacings
          arguments:
            - "@"
    sloglint:
      no-global: all
      context: scope
      static-msg: true
      key-naming-case: snake
    staticcheck:
      checks:
        - all

  exclusions:
    generated: strict
    warn-unused: true
    rules:
      - path: "_test\\.go$"
        linters:
          - cyclop
          - forbidigo
          - maintidx
          - noctx
      - path: "^(cmd/(spice|spice-release)|internal/qualitygate)/main\\.go$"
        text: 'use of `os\.Exit`'
        linters:
          - forbidigo
      - path: "^examples/commerce/main\\.go$"
        text: 'use of `os\.Exit`'
        linters:
          - forbidigo
      # Petclinic is a committed consumer fixture; its local replacement is
      # the mechanism that tests the unpublished framework tree.
      - path: "^examples[/\\\\]petclinic[/\\\\]go\\.mod$"
        text: "local replacement are not allowed"
        linters:
          - gomoddirectives
formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    gofumpt:
      module-path: github.com/StevenBuglione/spice

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  uniq-by-line: true

output:
  sort-order:
    - file
    - linter
